# 项目工作管理规范

> 规范类型：开发规范
> 规范说明：规定项目目标制定、任务拆分、自动实施、验收、归档与审计的管理要求。
> 最后修改日期：2026-08-02

本文档以目标作为用户授权和验收单位，以任务作为目标实施单位。用户与 AI 先共同确定目标及计划；用户启动目标后，AI 在目标边界内自主拆分并逐个完成任务，形成可追踪的文档、验证与 Git 记录，最后提交用户验收目标。

## 1. 适用范围

业务需求、项目优化和问题缺陷均可纳入工作管理。只有用户明确要求将事项作为目标或任务管理时，才建立工作记录；一般咨询、解释、检查或临时操作默认不进入工作管理。

工作管理包含三类对象：

- **目标**：描述要实现的结果、实施方案、任务规划和验收要求，是用户授权启动、验收完成和指定归档的单位。
- **AI 任务**：由 AI 负责实施并能自行完成必要验证的工作，是目标的主要执行单元。
- **用户任务**：必须由用户提供外部依赖或交付物的工作，例如准备服务器、域名、账号、权限或业务资料。

每个任务必须关联一个目标。无法关联目标的既有审计记录可以使用 `—`，新任务不得使用该例外。

## 2. 核心原则

1. **目标授权**：用户通过“计划启动”批准目标。目标启动前不得实施目标范围内的任务；目标启动后，AI 无需逐项申请任务审批。
2. **边界内自主执行**：AI 可以在已批准的目标范围内新增、拆分、排序、启动、阻塞、恢复、完成和取消 AI 任务，并逐个实施与验证。
3. **用户依赖显式化**：需要用户提供外部依赖或交付物时建立用户任务，不得将尚未取得的用户交付伪装为 AI 已完成事项。
4. **目标级验收**：AI 完成目标所需工作和验证后只能提交待验收；目标完成必须由用户明确验收通过。
5. **终态不可重开**：已完成或已取消的目标、AI 任务和用户任务不得重新打开。后续修改通过新增任务或新增目标处理。
6. **验收修改新增任务**：用户对待验收目标提出目标范围内的修改时，新增任务处理，原已结束任务保持不变；超出原目标范围的要求应建立新目标。
7. **单一位置**：同一目标或任务只能位于活动索引或归档索引中的一处。详情文档是配套记录，不属于重复登记。
8. **文档与实现一致**：实施涉及规范文件、项目文档文件或项目功能文件时，应按目标和任务计划同步维护并完成验证。
9. **变更隔离**：不得在目标或任务提交中混入用户原有改动或其他工作改动；无法安全隔离时必须暂停并说明。
10. **审计可追踪**：编号不复用，记录按时间追加，任务结果、验证证据、Git 提交和归档时间应可回溯。
11. **默认中文**：目标、任务、评审记录、代码注释和提交说明默认使用中文。

目标授权不替代对生产操作、破坏性操作、外部发布、资金支出、敏感信息使用或其他高风险行为所需的单独授权。

## 3. 目录与文件

```text
work/
├── task.md                    # 未归档的 AI 任务和用户任务
├── task-archive.md            # 已归档的 AI 任务和用户任务
├── target.md                  # 未归档目标
├── target-archive.md          # 已归档目标
├── tasks/
│   └── 编号-标题.md           # AI 任务详情文档
└── targets/
    └── 编号-标题.md           # 目标计划与交付文档
```

规范入口固定为 `spec/development/work/work-management-spec.md`。

目标文档和 AI 任务文档使用 `编号-标题.md` 命名，编号不补零。标题必须语义明确，不得包含 `/`、`\`、`:`、`|`、换行或首尾空格。分配编号后，标题默认冻结；确需修改时，应同步修改索引、文件名、文档标题、关联链接和后续提交消息。

用户任务不创建详情文档，只通过 `work/task.md` 和 `work/task-archive.md` 跟踪。

## 4. 时间、状态与编号

### 4.1 时间

- 所有时间使用 `Asia/Shanghai` 时区，格式为 `YYYY-MM-DD HH:mm`。
- 记录时间取目标或任务首次写入索引的实际时间，之后保持不变。
- 结束时间取目标或任务进入“已完成”或“已取消”的实际时间。
- 归档时间取记录迁入归档索引的实际时间。
- 尚未发生的时间或没有适用内容时统一写 `—`，不得填写预计值。

### 4.2 目标状态

| 状态 | 含义 |
| --- | --- |
| 未启动 | 目标已登记，正在讨论或等待用户批准计划启动 |
| 进行中 | 用户已启动目标，AI 正在拆分或实施任务 |
| 阻塞中 | 整个目标因外部依赖、权限、信息或环境问题无法继续 |
| 待验收 | 目标范围内的必要任务和 AI 可执行验证均已完成，等待用户验收 |
| 已完成 | 用户已明确验收通过 |
| 已取消 | 用户已明确取消目标 |

只有整个目标无法继续时才进入“阻塞中”。单个任务阻塞但仍有其他可执行任务时，目标保持“进行中”。

### 4.3 AI 任务状态

| 状态 | 含义 |
| --- | --- |
| 未启动 | 任务已记录但尚未实施 |
| 进行中 | AI 正在实施或验证任务 |
| 阻塞中 | 任务因依赖、权限、信息或环境问题暂时无法继续 |
| 已完成 | 任务范围内的实施和 AI 可执行验证均已完成 |
| 已取消 | 任务不再需要实施，取消原因已记录 |

AI 任务没有独立的用户验收状态。AI 根据任务完成标准和验证结果自行完成任务，用户在目标层面统一验收。

### 4.4 用户任务状态

| 状态 | 含义 |
| --- | --- |
| 未完成 | 用户尚未提供符合要求的交付内容 |
| 已完成 | 用户已经提供交付内容并由 AI 如实记录 |
| 已取消 | 用户任务不再需要，取消原因已记录 |

用户任务不细分未启动、进行中和阻塞中。用户任务阻断整个目标时，通过目标的“阻塞中”状态表达。

### 4.5 编号

目标、AI 任务和用户任务分别独立编号，均从 `1` 开始：

1. 目标编号扫描 `target.md`、`target-archive.md` 和 `targets/` 中的目标文档后，取历史最大编号加 `1`。
2. AI 任务编号扫描 `task.md`、`task-archive.md` 和 `tasks/` 中的 AI 任务文档后，取历史最大编号加 `1`。
3. 用户任务编号扫描 `task.md` 和 `task-archive.md` 中的用户任务后，取历史最大编号加 `1`。
4. 编号在记录创建时分配，一经分配永久不变，不得复用、回填空缺或因归档重新计算。

## 5. 工作索引

### 5.1 `target.md`

`target.md` 只包含一个目标表格，字段顺序固定为：

| 目标编号 | 目标标题 | 目标描述 | 目标状态 | 目标记录时间 | 目标结束时间 |
| --- | --- | --- | --- | --- | --- |

目标标题必须链接到对应目标文档，例如 `[建立账户认证能力](targets/1-建立账户认证能力.md)`。

### 5.2 `target-archive.md`

`target-archive.md` 只包含一个目标归档表格，字段顺序固定为：

| 目标编号 | 目标标题 | 目标描述 | 目标状态 | 目标记录时间 | 目标结束时间 | 目标归档时间 |
| --- | --- | --- | --- | --- | --- | --- |

归档目标只能保持“已完成”或“已取消”状态，目标标题继续链接到 `targets/` 中的文档。

### 5.3 `task.md`

`task.md` 只能包含“AI 任务”和“用户任务”两个二级章节，每个章节仅包含对应表格。

AI 任务字段顺序固定为：

| 任务编号 | 任务标题 | 任务描述 | 任务状态 | 关联目标 | 任务记录时间 | 任务结束时间 |
| --- | --- | --- | --- | --- | --- | --- |

用户任务字段顺序固定为：

| 任务编号 | 任务标题 | 任务描述 | 任务状态 | 任务结果 | 关联目标 | 任务记录时间 | 任务结束时间 |
| --- | --- | --- | --- | --- | --- | --- | --- |

AI 任务标题必须链接到 `tasks/` 中的文档。关联目标使用“编号-标题”并链接到 `targets/` 中的文档。用户任务的“任务结果”在已完成时记录用户交付，在已取消时记录取消原因，未完成时写 `—`。

### 5.4 `task-archive.md`

`task-archive.md` 同样只能包含“AI 任务”和“用户任务”两个二级章节，每个章节仅包含对应表格。

AI 任务归档表在 `task.md` 字段后增加“任务归档时间”：

| 任务编号 | 任务标题 | 任务描述 | 任务状态 | 关联目标 | 任务记录时间 | 任务结束时间 | 任务归档时间 |
| --- | --- | --- | --- | --- | --- | --- | --- |

用户任务归档表在 `task.md` 字段后增加“任务归档时间”：

| 任务编号 | 任务标题 | 任务描述 | 任务状态 | 任务结果 | 关联目标 | 任务记录时间 | 任务结束时间 | 任务归档时间 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |

只有已完成或已取消的任务可以进入归档表。

### 5.5 表格通用要求

- 新记录追加到对应表格最后一行，不按编号或时间重新排序。
- 每个目标或任务只占一行，单元格内换行使用 `<br>`。
- 单元格内容中的 `|` 写为 `\|`。
- 无值字段统一写 `—`，不得留空。
- 迁移记录时先从源表删除，再追加到目标表，最终不得同时存在于两个索引。
- 修改前必须重新读取四个索引和相关详情目录，检查编号、链接、状态与并发变化。

## 6. 目标文档

目标文档是用户与 AI 共同确定目标、授权执行和验收结果的完整记录。文档固定保留以下五个章节：

```md
# 编号-目标标题

## 1. 计划目标

## 2. 计划方案

## 3. 任务规划

## 4. 目标交付

## 5. 评审记录

| 评审结论 | 评审内容 | 评审时间 |
| --- | --- | --- |
```

编写要求：

- 目标文档重点在内容完整、事实准确和表达清楚，不规定各章内部的小节、表格或其他排版格式。AI 应根据目标内容选择适合的段落、列表、表格或图示，不为满足形式而拆分内容。
- “计划目标”根据用户与 AI 的讨论，全面说明目标要做成什么样子，并明确足以约束实施和验收的目标边界与结果要求。
- “计划方案”根据计划目标和项目当前事实，说明目标的整体实施方案，不得伪造尚未确认的项目事实。
- “任务规划”根据计划方案说明任务如何拆分，明确 AI 任务、用户任务以及必要的依赖关系和执行顺序。任务正式记录后，应建立对应索引和详情文档链接。
- “目标交付”说明目标交付的整体情况以及各任务的交付和验收情况，使用户能够据此验收目标；尚未提交验收时写“尚未发生”。
- “评审记录”只记录用户对目标计划、验收、取消和归档的明确意见，不记录 AI 自检或一般过程说明。
- 评审结论只允许“计划修改”“计划启动”“验收修改”“验收通过”“计划取消”“计划归档”。记录按时间追加，不得覆盖已有记录。
- 尚无用户评审时，评审表只保留表头。
- 不得在目标文档中记录密码、密钥、令牌、个人敏感信息或生产隐私数据。

目标处于“未启动”时，用户与 AI 共同完善计划目标、计划方案和任务规划，尚未讨论确定的内容可以明确写明尚待讨论。进入“进行中”前，这三个章节必须完整，不得保留影响实施边界或执行方式的待定项。

## 7. AI 任务文档

AI 任务文档用于记录任务实施计划、实际改动、验证和交付，固定结构如下：

```md
# 编号-任务标题

## 1. 任务信息

| 字段 | 内容 |
| --- | --- |
| 任务编号 | 1 |
| 任务标题 | 标题 |
| 任务描述 | 描述 |
| 关联目标 | 编号-标题 |
| 当前状态 | 未启动 / 进行中 / 阻塞中 / 已完成 / 已取消 |
| 基线分支 | dev |
| 目标分支 | dev |
| 任务分支 | codex/任务分支 / 不适用 |
| 工作树 | 路径 / 不适用 |

## 2. 实施计划大纲

### 2.1 规范文件大纲
### 2.2 项目文档大纲
### 2.3 项目功能大纲

## 3. 规范文件计划

## 4. 项目文档计划

### 4.1 项目文档实施计划
### 4.2 无需更新与判断依据

## 5. 项目功能计划

### 5.1 目标
### 5.2 非目标
### 5.3 执行步骤
### 5.4 风险与回退
### 5.5 完成标准
### 5.6 验证方式

## 6. 实施记录

### 6.1 实际改动
### 6.2 计划偏差
### 6.3 验证命令与结果

## 7. 交付结果

### 7.1 交付提交
### 7.2 合入结果
### 7.3 工作树与分支清理结果

## 8. 阻塞或取消记录
```

编写要求：

- 任务文档不记录任务优先级、记录时间、结束时间和归档时间，这些信息统一由索引维护。
- 任务文档不设置计划评审、任务验收和下一步任务建议。AI 在已启动目标范围内自行制定任务实施细节和完成任务，用户统一验收目标。
- 实施计划必须受关联目标约束，不得改变目标范围、交付物或验收标准。
- “规范文件计划”和“项目文档计划”不得省略；无需修改时，应写明“无”或列明检查范围与判断依据。
- 完成标准和验证方式必须具体，不得使用“功能正常”“没有问题”等无法核验的表述。
- 实施后如实记录实际改动、计划偏差、验证命令与结果以及交付情况。
- 阻塞记录应说明原因、已尝试措施和解除条件；取消记录应说明取消原因。
- 非表格章节无内容时写“无”或“尚未发生”，不得删除固定章节。
- 已经形成的审计文档应保留其事实内容，不得为了套用模板而删除有效记录或改写历史事实。

## 8. 目标流程

### 8.1 登记与讨论

1. 用户明确要求将事项作为目标管理后，确认目标标题和已知描述。
2. 检查是否与未归档目标重复；疑似重复时先说明关系，再由用户决定补充现有目标或创建新目标。
3. 分配目标编号，在 `target.md` 以“未启动”状态登记，并创建目标文档。
4. 创建 `目标-登记-编号-标题` 提交。
5. 用户与 AI 围绕计划目标、计划方案、任务规划和验收标准讨论；用户提出修改时追加“计划修改”评审记录。

未引起状态变化的讨论和文档更新不强制单独提交，应在下一次管理提交中一并保存。用户明确要求保留中间版本时可以单独提交。

### 8.2 启动目标

只有用户明确给出“计划启动”结论后才能启动目标：

1. 确认计划目标、计划方案、任务规划和验收标准完整且无待定项。
2. 在评审记录中追加“计划启动”。
3. 将目标状态从“未启动”改为“进行中”。
4. 根据任务规划登记初始 AI 任务和用户任务，并建立 AI 任务文档。
5. 创建目标启动及任务登记对应的管理提交。
6. AI 按依赖顺序自主选择并逐个执行 AI 任务，不再等待逐任务批准。

### 8.3 阻塞与恢复

- 单个 AI 任务无法继续时，记录阻塞原因和解除条件，将任务改为“阻塞中”，创建任务阻塞提交，然后继续其他不受影响的任务。
- 所有可执行路径均被阻断时，将目标改为“阻塞中”，创建目标阻塞提交并向用户说明所需条件。
- 条件满足后，恢复对应任务；目标重新具备可执行路径时恢复为“进行中”。恢复动作必须更新记录并提交。
- 阻塞源是用户交付时，应建立或更新对应用户任务。

### 8.4 提交目标验收

同时满足以下条件后，AI 才能将目标提交待验收：

- 目标范围内所有必要 AI 任务均已完成，不再需要的任务均已取消且原因明确。
- 所有必要用户任务均已完成；用户任务取消不会破坏目标验收标准。
- 相关规范文件、项目文档文件和项目功能文件已经同步。
- AI 能执行的验证全部完成并通过，无法由 AI 执行的检查已列为用户验收项。
- 目标文档“目标交付”已填写完整，任务记录、提交与实现一致。

满足条件后，将目标从“进行中”改为“待验收”，创建 `目标-待验收-编号-标题` 提交，并向用户说明完成内容、验证结果、已知限制和待验收项。该动作不写入用户评审记录。

### 8.5 验收修改

用户对待验收目标提出修改时：

1. 在目标评审记录中追加“验收修改”，准确记录意见。
2. 判断反馈是否仍属于原目标范围。超出范围时建立新目标，不扩展当前目标。
3. 对范围内反馈新增 AI 任务或用户任务，不修改或重新打开已完成、已取消任务。
4. 更新计划方案、任务规划和目标交付，将目标从“待验收”恢复为“进行中”。
5. 创建目标恢复及新增任务对应的提交，然后自主完成新增任务并重新提交验收。

用户给出的验收修改本身构成对原目标范围内修改的授权，不需要再次逐任务审批。修改要求改变目标结果、范围、交付物或验收标准时，不得扩展当前目标，应另行登记并讨论新目标。

### 8.6 验收通过与完成

用户明确验收通过后：

1. 在目标评审记录中追加“验收通过”。
2. 确认目标交付和任务记录完整，交付提交已位于目标分支。
3. 将目标从“待验收”改为“已完成”，填写目标结束时间。
4. 创建 `目标-完成-编号-标题` 提交。

AI 不得以自行验证通过代替用户验收结论。

### 8.7 取消目标

只有用户明确要求时才能取消目标：

1. 在评审记录中追加“计划取消”并记录原因。
2. 停止目标实施，逐项取消尚未结束的 AI 任务和用户任务。
3. 已存在的项目改动不得自动删除、回滚或擅自提交；应列明变更并按用户意见保留、转交新目标或安全撤销。
4. 所有任务进入终态后，将目标改为“已取消”，填写目标结束时间。
5. 创建任务取消和 `目标-取消-编号-标题` 提交。

## 9. 任务流程

### 9.1 登记任务

- 任务只能从目标的计划方案和任务规划拆分产生。
- AI 根据目标当前已知信息判断任务责任方：能够由 AI 实施并验证的建立为 AI 任务，必须由用户提供外部交付的建立为用户任务。
- 任务描述应说明具体结果和边界；依赖关系与执行顺序记录在目标文档中。
- AI 任务登记时分配编号，在 `task.md` 写入“未启动”，创建任务文档，并创建 `AI任务-登记-编号-标题` 提交。
- 用户任务登记时分配编号，在 `task.md` 写入“未完成”，并创建 `用户任务-登记-编号-标题` 提交。
- 用户可以补充或调整任务；AI 也可以在目标范围内根据实施发现新增、拆分或取消任务，无需逐项请求批准。

### 9.2 实施 AI 任务

1. 按目标任务规划、依赖关系和当前项目事实选择下一个任务。默认同一时间只实施一个 AI 任务；任务阻塞后可以切换到其他可执行任务。
2. 完善任务实施计划，将状态改为“进行中”，创建 `AI任务-启动-编号-标题` 提交。
3. 按计划修改规范文件、项目文档文件和项目功能文件，执行验证并回填实施记录。
4. 实施细节可以根据项目事实调整；改变目标范围、交付物或验收标准时必须停止并回到目标讨论。
5. 完成标准和验证全部满足后，将任务改为“已完成”，填写任务结束时间，并创建 `AI任务-完成-编号-标题` 提交。

AI 任务完成提交可以包含该任务的规范、文档、功能、验证记录和任务管理记录。较长任务允许创建中间实现提交，但不作强制要求；中间提交不得破坏任务变更隔离。

### 9.3 AI 任务阻塞、恢复与取消

- 阻塞时更新任务文档，将状态改为“阻塞中”，创建 `AI任务-阻塞-编号-标题` 提交。
- 阻塞解除后将状态恢复为“进行中”，补充解除说明，创建 `AI任务-恢复-编号-标题` 提交。
- 取消任务时记录明确原因，将状态改为“已取消”，填写任务结束时间，创建 `AI任务-取消-编号-标题` 提交。
- AI 只能在目标范围调整、任务被替代、已不再需要或无法在目标内成立时自主取消任务，不得用取消规避未完成工作。

### 9.4 用户任务完成与取消

- 用户提供交付后，AI 将交付内容如实写入“任务结果”，把状态改为“已完成”，填写任务结束时间，创建 `用户任务-完成-编号-标题` 提交。
- AI 能够从项目或环境中验证交付时应执行必要检查；无法验证时应明确记录用户提供的内容，不得宣称已经验证。
- 用户明确取消或已批准的目标调整使用户任务不再需要时，在“任务结果”记录原因，把状态改为“已取消”，填写任务结束时间，创建 `用户任务-取消-编号-标题` 提交。

## 10. 归档

### 10.1 归档任务

用户指定归档某个已完成或已取消任务时：

1. 确认任务处于终态且只存在于 `task.md`。
2. 从 `task.md` 删除任务行，追加到 `task-archive.md` 对应章节并填写任务归档时间。
3. AI 任务文档继续保留在 `tasks/`，不得移动或删除。
4. 根据任务类型创建 `AI任务-归档-编号-标题` 或 `用户任务-归档-编号-标题` 提交。

单独归档任务不影响关联目标状态。

### 10.2 归档目标

用户只能指定归档已完成或已取消的目标：

1. 确认目标下所有任务均已完成或已取消；存在非终态任务时不得归档。
2. 在目标评审记录中追加“计划归档”。
3. 将仍位于 `task.md` 的关联任务迁入 `task-archive.md` 并填写各自归档时间；已经归档的关联任务保持原位。
4. 将目标从 `target.md` 迁入 `target-archive.md` 并填写目标归档时间。
5. `tasks/` 和 `targets/` 中的详情文档永久保留。
6. 创建任务归档提交和 `目标-归档-编号-标题` 提交。

归档不是状态，目标和任务在归档索引中继续保持原终态。

## 11. Git 提交

### 11.1 提交消息

提交消息统一使用以下格式，不增加正文或其他前后缀：

```text
目标-操作-编号-标题
AI任务-操作-编号-标题
用户任务-操作-编号-标题
```

允许的操作如下：

| 对象 | 操作 |
| --- | --- |
| 目标 | 登记、启动、阻塞、恢复、待验收、完成、取消、归档 |
| AI 任务 | 登记、启动、阻塞、恢复、完成、取消、归档 |
| 用户任务 | 登记、完成、取消、归档 |

登记、状态变化和归档必须创建对应提交。未引起这些管理动作的文档完善和实现过程不强制单独提交，但应在下一次相关提交中保存；必要的中间实现提交可以按项目 Git 习惯创建。

### 11.2 提交边界

- 每个提交只包含对应目标或任务范围内的文件，不得混入其他工作。
- 提交前必须查看工作树和暂存区差异，逐个指定文件或明确目录暂存，禁止使用可能混入无关改动的宽泛暂存方式。
- 如果同一文件存在无法区分归属的其他改动，应暂停提交并说明。
- 目标管理提交只包含该目标状态、目标文档、必要索引和与该动作不可分割的任务登记记录。
- AI 任务完成提交可以同时包含该任务实现与管理记录，确保提交与任务结果对应。
- 创建本地提交不代表获得推送、创建 Pull Request、修改远程历史或执行发布的授权。

## 12. 分支、工作树与并发

- 默认基线分支和目标分支均为 `dev`；仓库实际工作流另有明确规定时按项目事实填写。
- 单个任务可以在当前工作树实施。存在用户改动、其他任务改动或并行实施需要时，应使用独立分支和工作树。
- 分支默认使用 `codex/` 前缀，并在任务文档中记录实际名称和工作树路径。
- 同一时间默认只实施一个 AI 任务。确需并行时，每个任务必须使用独立分支和工作树，且不得同时修改工作索引。
- 多个 AI 会话不得并发修改 `work/` 下的索引和详情文档。
- 合入前重新核对目标分支和任务范围；冲突涉及业务含义或其他工作时必须暂停，不得擅自选择一方覆盖。
- 工作树和分支只在交付已经安全合入且工作树无未提交改动后清理，不得强制删除未确认内容。

## 13. 一致性与异常处理

- 每次管理动作前重新读取四个索引和相关详情文档，确认源状态、编号、链接和文件没有发生并发变化。
- 目标状态、任务状态、详情文档、实现和 Git 记录必须一致；发现差异时先恢复一致性，再继续流转。
- 文件写入、验证、提交或合入失败时，不得宣称对应状态变化已经成功。
- 索引迁移失败时优先恢复源记录，确保最终只位于一个索引。
- 任务完成后发现问题，必须新增任务，不得改写原任务完成事实。
- 目标完成后发现属于原目标范围的问题，应建立关联的新目标；不得重新打开原目标。
- 更正错误记录时应保留可理解的修正说明和 Git 轨迹，不得覆盖、删除或伪造审计事实。
- 工作记录不得包含密码、密钥、令牌、个人敏感信息或生产隐私数据。

## 14. 操作检查

### 目标启动前

- [ ] 目标结果、范围、非目标、交付物和验收标准完整。
- [ ] 计划方案基于当前项目事实，规范、文档、验证和风险边界明确。
- [ ] AI 任务、用户任务、依赖关系和执行顺序清楚。
- [ ] 用户已明确给出“计划启动”。

### AI 任务完成前

- [ ] 实际改动没有超出目标和任务范围。
- [ ] 相关规范文件和项目文档已经同步或有明确无需更新依据。
- [ ] 完成标准已满足，AI 可执行验证已经通过并记录。
- [ ] 未混入其他任务或用户原有改动。

### 目标待验收前

- [ ] 必要任务全部进入终态，用户依赖已经满足。
- [ ] 目标验收标准逐项具备证据。
- [ ] 目标交付、任务记录、实现和提交一致。
- [ ] 已知限制和待用户验收项已经明确说明。

### 归档前

- [ ] 目标或任务处于“已完成”或“已取消”。
- [ ] 关联记录不存在非终态遗漏或活动索引与归档索引重复。
- [ ] 用户已经明确指定归档对象。
- [ ] 详情文档保持可访问，归档时间按实际动作填写。
